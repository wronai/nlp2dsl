{
  "name": "mvp-voice-chat-wrapper",
  "private": true,
  "version": "0.1.0",
  "description": "Tauri desktop wrapper for the existing FastAPI voice chat UI",
  "scripts": {
    "dev": "node scripts/dev.js",
    "desktop": "bash ./desktop.sh",
    "serve": "node scripts/serve-dist.js",
    "build": "tauri build",
    "icon": "tauri icon"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^1.5.0"
  }
}
